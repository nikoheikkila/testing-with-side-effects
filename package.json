{
	"name": "testing-with-side-effects",
	"module": "app.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@biomejs/biome": "2.2.6",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"scripts": {
		"format": "biome check --write",
		"compile": "bun build --compile --outfile=dist/app cli.ts"
	}
}
